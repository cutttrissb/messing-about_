<%
                
  %>

The product loads screen

# So the key now is to see whether we can instantiate some items - notably the
# wiggle and pbk classes and call the gdi.save method upon them to see what happens :)






